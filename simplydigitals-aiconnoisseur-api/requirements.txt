# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — production dependencies for simplydigitals-aiconnoisseur-api
#
# All direct AND implicit dependencies are listed explicitly here.
# This prevents platform-specific install failures (especially macOS ARM)
# where optional sub-dependencies are not automatically pulled in.
#
# Install:  pip install -r requirements.txt
# Dev/test: pip install -r requirements.txt -r requirements-dev.txt
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
starlette>=0.37.0
anyio>=4.0.0

# ORM & Database
sqlalchemy[asyncio]>=2.0.30
alembic>=1.13.1
aiosqlite>=0.20.0
greenlet>=3.0.0            # Required by SQLAlchemy async on all platforms
psycopg2-binary>=2.9.9     # PostgreSQL driver (production)

# Auth & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]==1.7.4     # Pin to 1.7.4 — newer versions break bcrypt compat
bcrypt==4.0.1              # Pin to 4.0.1 — passlib is incompatible with bcrypt 4.1+
                           # bcrypt 4.1+ removed __about__ which passlib reads
slowapi>=0.1.9
limits>=3.6.0              # Required by slowapi

# Validation
pydantic>=2.7.0
pydantic-settings>=2.2.1
pydantic-core>=2.18.0      # Compiled core for pydantic v2
email-validator>=2.1.1
dnspython>=2.6.0           # Required by email-validator

# ML / Data Science
scikit-learn>=1.4.2
pandas>=2.2.2
numpy>=1.26.4
joblib>=1.4.0
threadpoolctl>=3.1.0       # Required by scikit-learn
scipy>=1.13.0              # Required by scikit-learn algorithms

# Observability
structlog>=24.1.0
prometheus-fastapi-instrumentator>=6.1.0
prometheus-client>=0.20.0  # Required by prometheus-fastapi-instrumentator

# Utilities
python-dateutil>=2.9.0     # Required by pandas
pytz>=2024.1               # Required by pandas
six>=1.16.0                # Required by python-dateutil
click>=8.1.0               # Required by uvicorn CLI
h11>=0.14.0                # Required by uvicorn
Mako>=1.3.0                # Required by alembic templates
